--  ╭──────────────────────────────────────────────────────────╮
--  │ Marty Buchaus Neovim Configuration                       │
--  ╰──────────────────────────────────────────────────────────╯

return {
    -- "ca-mantis-shrimp/Todoist.nvim",
    -- lazy = false,
    -- dependencies = {
    --     "nvim-lua/plenary.nvim",
    --     "ca-mantis-shrimp/tree-sitter-projects",
    -- },
    -- config = { api_key = vim.env.TODOIST_TOKEN },

    -- "romgrk/todoist.nvim",
    -- cmd = {
    --     "Todoist",
    --     "TodoistInstall"
    -- },
    -- config = function()
    --     require("todoist").setup({
    --         api_key = vim.env.TODOIST_API_KEY,
    --     })
    -- end,
}
